<%= form_with(model: log_activity) do |form| %>
  <% if log_activity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(log_activity.errors.count, "error") %> prohibited this log_activity from being saved:</h2>

      <ul>
        <% log_activity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_field :text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
